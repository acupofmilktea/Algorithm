#include <iostream>
#include <string>
using namespace std;

int main() {
    string N, M;
    cin>>N;

    while(!N.compare(M)) {
        int temp;
        if(N.length()==2) {
            temp = stoi()
            stoi(N[1])
        }
    }

    return 0;
}