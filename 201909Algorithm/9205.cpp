#include <iostream>
#include <algorithm>
using namespace std;
//플로이드-와샬, bfs
int main() {
    ios_base::sync_with_stdio(0);
    cin.tie();
    int t;
    cin>>t;
    while(t>0){
        int n, beer=20;
        cin>>n;
        int con[n][3], hx, hy, px, py;
        cin>>hx>>hy;
        for(int i=0; i<n; i++)
            cin>>con[i][0]>>con[i][1], con[i][2]=40000;
        cin>>px>>py;
        for(int i=0; i<n; i++){

        }

        t--;
    }
    return 0;
}